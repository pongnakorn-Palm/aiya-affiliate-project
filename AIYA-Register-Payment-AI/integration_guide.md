# AIYA Ticket System - Integration Guide

คู่มือการเชื่อมต่อสำหรับ Developer เพื่อสร้าง "Direct Link" จากเว็บไซต์หลัก มายังหน้าชำระเงิน (Ticket Purchase)

## Concept
เว็บไซต์หลักสามารถส่งลูกค้ามายังหน้าชำระเงิน พร้อม **ระบุแพ็กเกจ** และ **กรอกข้อมูลลูกค้าล่วงหน้า** (Pre-fill) โดยการแนบ Query Parameters มากับ URL

## Base URL
```
https://aiya-bootcamp.vercel.app/tickets
```

## Supported Parameters (ข้อมูลที่ส่งมาได้)

| Parameter | Type | Values | Description |
| :--- | :--- | :--- | :--- |
| `package` | string | `SINGLE`, `DUO` | แพ็กเกจที่ต้องการเลือก (Case-insensitive) |
| `name` | string | (Free text) | ชื่อ-นามสกุล ของลูกค้า |
| `email` | string | (Valid Email) | อีเมลของลูกค้า |
| `phone` | string | (Phone Number) | เบอร์โทรศัพท์ |
| `referral` | string | (Code) | รหัสส่วนลด / Referral Code (ถ้ามี) |

---

## 💡 Example Usage (ตัวอย่างการใช้งาน)

### 1. ลิงก์แบบเลือกแพ็กเกจอย่างเดียว
ลูกค้าเลือกแพ็กเกจ DUO มาจากหน้าแรก ยังไม่รู้ชื่อลูกค้า
```
https://aiya-bootcamp.vercel.app/tickets?package=DUO
```

### 2. ลิงก์แบบจัดเต็ม (Pre-fill ทุกอย่าง)
ลูกค้า Logged-in อยู่แล้ว ต้องการส่งข้อมูลมาให้ครบเลย เพื่อให้กดจ่ายเงินได้ทันที
```
https://aiya-bootcamp.vercel.app/tickets?package=SINGLE&name=Somchai+Jaidee&email=somchai@example.com&phone=0812345678&referral=PROMO2026
```

---

## 💻 Tech Implementation Note (สำหรับ Dev)

เมื่อUser กดปุ่มที่เว็บไซต์หลัก ให้ทำการ Generate URL โดยแทนที่ค่าต่างๆ ด้วยข้อมูลจริงของ User นั้นๆ
**Example (JavaScript / React):**

```javascript
const user = {
  name: "Somsri Rakdee",
  email: "somsri@gmail.com",
  phone: "0998887777"
};

const baseUrl = "https://aiya-bootcamp.vercel.app/tickets";
const params = new URLSearchParams({
  package: "DUO",
  name: user.name,
  email: user.email,
  phone: user.phone,
  referral: "AIYA_WEB"
});

// Redirect user
window.location.href = `${baseUrl}?${params.toString()}`;
```

---

**หมายเหตุ:**
- หาก Parameter ไหนไม่มี หรือเป็นค่าว่าง สามารถไม่ส่งมาได้ ระบบจะปล่อยว่างให้ลูกค้ากรอกเอง
- ค่าภาษาไทย หรือเว้นวรรค ควรทำ URL Encoding มาด้วย (ตามมาตรฐานปกติ)
